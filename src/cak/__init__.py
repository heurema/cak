"""CAK v0.1 — typed tool-boundary governance.

Scope per docs/13 and docs/17: typed effects, pre-exec verification,
explainable decisions, trace recording, semantic replay. Gateway table
stakes (authn/authz engines, masking, rate limiting) are out of scope.
"""

__version__ = "0.1.0"
