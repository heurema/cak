# 12 Memory Update

No long-term memory update requested by the user.

Candidate memory only if explicitly requested later:

- CAK WASM architecture verdict: keep Rust core; reserve WASM as a future
  optional sandboxed evaluator ABI; validate via separate `cak-wasm-runner`
  spike before adding Wasmtime/Extism dependencies.
